# Presentation IR schema

`presentation.schema.json` is the language-neutral contract for Presentation IR v1. The FastAPI Pydantic models are the executable server contract; the frontend mirrors this shape in TypeScript. Coordinates and dimensions are inches to align with editable PowerPoint geometry.

